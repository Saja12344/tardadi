name: tardadi_driver
description: ترددي — تطبيق السائق
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_map: ^8.1.1
  geolocator: ^14.0.2
  latlong2: ^0.9.1
  shared_preferences: ^2.5.3
  tardadi_core:
    path: ../../packages/tardadi_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/vehicles/
